# mark-os-go

Static deep-link bouncers for Mark OS: Notion only accepts https links, so
card links that should open the Claude app point here and get redirected to
the claude:// scheme (browser fallback after 1.5s). Served by GitHub Pages.

- `/alfred/` → the Talk-to-Alfred claude.ai project
